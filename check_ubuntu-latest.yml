name: Check ubuntu-latest resources

on:
  push:

jobs:
  report:
    name: Report runner resources (ubuntu-latest)
    runs-on: ubuntu-latest

    steps:
      - name: System info
        run: |
          uname -a
          cat /etc/os-release
          lscpu
          free -h
          df -h
